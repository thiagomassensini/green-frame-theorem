# Green Frame Theorem v1.0.1

Maintenance publication of the audited Green Frame formal core and source bundle.

## Changes since v1.0.0

- align the Lake package, citation, and Zenodo metadata with `v1.0.1`;
- replace ambiguous paper-ID mappings with a conservative ledger that distinguishes exact, partial, abstract, interface-only, source-boundary, and future coverage;
- remove the temporary Actions diagnostics bridge used during the `v1.0.0` recovery;
- replace one-off audit-branch routing with the permanent pull-request and `main` gates;
- add a metadata-consistency gate and an immutable, draft-first `v1.0.1` publisher with exact target locking and downloaded-asset checksum verification.

## Formal guarantees

- the same 41 named Lean theorem statements and public API as `v1.0.0`;
- pinned Lean 4.32.0 and Mathlib 4.32.0;
- no placeholders, project axioms, or unsafe declarations;
- per-theorem axiom audit with the standard allowlist;
- unchanged byte-preserved paper specification with SHA-256 `366dfa007002c1d3dbf2eb7c283c13c1b35233647163753ee39e45dd0645c3f4`;
- exact source archive and checksummed audit evidence.

This patch changes publication and audit metadata only. It introduces no new mathematical or spectral claim.
