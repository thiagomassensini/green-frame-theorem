# Green Frame Theorem v2.0.0

> The 20 `KERNEL_CHECKED` rows are evidence only for the exact commit and tree
> whose successful seven-gate runs are recorded in the bundled audit manifest.

This release supersedes the historical `v1.0.0` test publication with the
remote-first concrete reconstruction required by the project contract.

## Included

- concrete positional carry depth and the canonical all-bases partition;
- the exact external `(seed,residual,G1)` / bulk `G>=2` coefficient split and
  the canonical `(base,n)=(2,4)` bulk witness on `e4`;
- concrete complex Green, residual, frame, normalization, and Poisson layers
  exactly as classified in the bundled machine-readable claim ledger;
- all 23 primary-paper claims assigned an exact release category: 20 are
  `KERNEL_CHECKED`, `ABGF-FS-003` is
  explicitly conditional on the named common-space CFC inverse-square-root
  limit, `ABGF-FS-004` is open without theorem evidence, and
  `ABGF-WEYL-001` is retained as the future spectral layer;
- pinned Lean and Mathlib revisions with a warning-free public-root build;
- a registry-derived `#print axioms` query for every public theorem;
- exact per-theorem axiom identities under the versioned standard allowlist;
- a complete import graph rooted at `GreenFrame.lean`;
- byte-for-byte SHA-256 verification of all 16 supplied research inputs;
- an exact-SHA/tree/run audit manifest and six release assets, with
  `SHA256SUMS.txt` covering the other five and its own digest bound into the
  immutable annotated-tag message.

Dynamic theorem, module, documentation, and evidence counts are recorded in
`audit-manifest.json`; they are not frozen into these release notes.

## Trust boundary

Markdown notes and Python laboratories are preserved research inputs.  They do
not become kernel evidence merely by being shipped.  Only the declarations
reachable from `GreenFrame.lean`, accepted by the pinned Lean kernel, present
in the public theorem registry, and covered by the bundled named axiom report
are classified as kernel checked.

The `v1.0.0` tag and release remain unchanged as historical material and are
not evidence for this v2 publication.
