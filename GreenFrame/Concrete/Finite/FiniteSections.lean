import GreenFrame.Concrete.Finite.ConcreteStrongAnalysisLimit
import GreenFrame.Concrete.Finite.ConcreteCutoffRanges

/-!
# Concrete finite analysis sections

Thin public aggregator for the acyclic `ABGF-FS-001`/`ABGF-FS-002` chain:

1. coordinate masks and literal cutoff predicates;
2. nested product cutoff maps;
3. seed-residual retention and uniform bounds;
4. Tannery tail limits in every `ℓ²` sector;
5. exact range identifications with `H_N` and finite retained rows;
6. the strong `Q_N T P_N` limit.
-/
